<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.fisk.findteacher">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:noHistory = "true" >

        <!-- actividades con sin tool bar por defecto -->
        <!--actividades con sin tool bar por defecto-->
        <activity
            android:name="org.fisk.findteacher.RegistroActivity"
            android:theme="@style/temasinactionbar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="org.fisk.findteacher.PrincipalActivity"
            android:theme="@style/temasinactionbar"></activity>
        <activity
            android:name="org.fisk.findteacher.LoginActivity"
            android:theme="@style/temasinactionbar"></activity>
        <!-- tema AppTeme -->
        <activity
            android:name="org.fisk.findteacher.RegistroDatosPersonales"
            android:label="@string/datos_personales" />
        <activity
            android:name="org.fisk.findteacher.PagoServicio"
            android:label="@string/realizacion_pago" />
        <activity android:name="org.fisk.findteacher.DemandaServicios" />
        <activity android:name="org.fisk.findteacher.DetalleUsuarioActivity" />
        <activity android:name="org.fisk.findteacher.ListadoPerfilesActivity" />
        <activity android:name="org.fisk.findteacher.RespuestaActivity" />
        <!-- otras -->
        <activity
            android:name="org.fisk.findteacher.AmpliarInfoActivity"
            android:label="Ampliar Información" />
        <activity
            android:name="org.fisk.findteacher.DemandasActivity"
            android:label="Demandas solicitadas"/>
        <!--tema AppTeme-->
        <activity android:name="org.fisk.findteacher.Perfil"
            android:label="Perfil del Formador"/><!-- esto decía antes Editar Perfil -->
        <activity
            android:name="org.fisk.findteacher.EnvioRespuestaPersonal"
            android:label="@string/tit_envio_respuesta" />
        <activity android:name="org.fisk.findteacher.ListadoCrearDemandas"
            android:label="Qué Necesitas?"   />
        <activity android:name="org.fisk.findteacher.DetalleCrearDemanda"
            android:label="Crear Demanda" />
        <activity android:name="org.fisk.findteacher.ListadoVerDemandas"
            android:label="Demandas" />
        <activity android:name="org.fisk.findteacher.DetalleVerDemanda"
            android:label="Mi Demanda" />
        <activity android:name="org.fisk.findteacher.ListadoVerMisDemandas"
        android:label="Mis Demandas" />
        <activity android:name="org.fisk.findteacher.DetalleConsultaMiDemanda"
            android:label="Consulta" />
        <activity android:name="org.fisk.findteacher.DetalleRespuesta"
            android:label="Respuesta" />
        <activity android:name="org.fisk.findteacher.DetalleVerDemandas"
            android:label="Demandas" />
        <activity android:name="org.fisk.findteacher.ListadoGestionPerfilesActivity"
            android:label="Mis Perfiles" />
        <activity android:name="org.fisk.findteacher.DetalleMiPerfilActivity"
            android:label="Mi Perfil" />
        <!--otras-->
    </application>

</manifest>